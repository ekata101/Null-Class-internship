# SQLite Database path (optional, defaults to db/app.db)
DB_PATH=./db/app.db

# JWT
JWT_SECRET=your_jwt_secret_here

# Email (Gmail SMTP for OTP, forgot password, and invoice emails)
EMAIL_USER=youremail@gmail.com
EMAIL_PASS=your_app_password

# Server
PORT=3000
NODE_ENV=development
